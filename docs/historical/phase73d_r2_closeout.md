# Phase 7.3D-R2A Closeout Report — Reachability-Corrected Zero-Tail Semantics & Exact Successor Solver

**Date**: July 22, 2026  
**Status**: **`VERIFIED_LOCAL_ZERO_LIFT_TRANSITION` & `SOUND_ACCELERATED_UNRANKED` (GLOBAL DYNAMICS)**  
**Crates Modified/Created**: `collatz-cegar`, `collatz-cert`, `collatz-affine`  
**Test Matrix**: 100% Rust workspace test suite, Python differential oracle (`scripts/zero_lift_oracle.py`, **0 diff**), 2-adic fixed point witness generator (`pure_zero_gap_witness(length)`, **100% PASSED** for $r=1,3,5,10$), regression vectors $z_0 = 200,534$ and $z_0 = 23,750,971,222$

---

## 1. Pure-$j=0$ Witness Theorem & Fixed Regression Vectors

### Theorem 1 (Arbitrarily Long Pure-$j=0$ Orbits & 2-Adic Fixed Point)
The accelerated map for branch 0 ($j=0$) is $f(z) = \frac{729 z + 26}{512}$.
Its unique 2-adic fixed point is $z^* = \frac{-26}{217} \in \mathbb{Z}_2$.
Because 217 is odd, $z^* \in \mathbb{Z}_2$, and $z^* \equiv 342 \pmod{512}$.
For any target length $r \ge 1$ and $N = 9(r+1)$, the modular representative:
$$z_r = [-26 \cdot 217^{-1}]_{2^N}$$
is a **positive integer state** executing **at least $r+1$ consecutive $j=0$ accelerated steps** (`CLM-P7X-PURE-ZERO-GAP-WITNESS-THEOREM-001`).

#### Proof of Arbitrary Length
Since $-217$ is odd and coprime to 2, it has an exact inverse modulo $2^N$ for any $N$.
$f^k(z_0) - z^* = \left(\frac{729}{512}\right)^k (z_0 - z^*)$. Because $z_0 \equiv z^* \pmod{2^{9(r+1)}}$, $f^k(z_0) \equiv z^* \equiv 342 \pmod{512}$ for all $k \le r$.

#### Fixed Regression Vectors
- **$r=1$ Witness**: $N=18$, $z_0 = 200,534 \implies 200,534 \to 285,526 \to 406,540$. Residues mod 512 are $342, 342, 12$ (executes **exactly 2 consecutive $j=0$ steps**).
- **$r=3$ Witness**: $N=36$, $z_0 = 23,750,971,222 \implies$ residues mod 512 are $342, 342, 342, 342, 441$ (executes **exactly 4 consecutive $j=0$ steps**).

---

## 2. Exact Successor-Solver Equivalence Theorem

### Theorem 2 (Combined Congruence & Exact Equivalence)
For any non-negative endpoint $y_s \ge 0$, $y_s \equiv C_j \pmod{M_j}$ if and only if, with $t = 1 + 11 y_s$:
$$27^j (231 + 729 t) \equiv 81 \cdot 2^{1+4j} \pmod{2^{9+4j}}$$

#### Proof
Since $27^j \cdot 729 = 3^{6+3j}$ is odd and coprime to 2, it is **strictly invertible modulo $M_j = 2^{9+4j}$**.
Therefore, this single combined congruence determines **exactly one unique residue** $t \equiv c_j \pmod{M_j}$.
Since 11 is also odd and coprime to 2, $11^{-1} \pmod{M_j}$ exists, uniquely recovering $y_s \equiv C_j \pmod{M_j}$.

---

## 3. Subsystem $J=0$ Pointwise Termination vs Unbounded Finite Runs

- **Theorem (Pure-$j=0$ Pointwise Termination)**: Every positive integer trajectory in the $J=0$ subsystem terminates in finite time (contains no infinite $0^\omega$ orbit), because the unique 2-adic fixed point $z^* = \frac{-26}{217} < 0$ is negative in $\mathbb{Q}$.
- **Theorem (Unbounded Finite Run Lengths)**: Arbitrarily long finite pure-$j=0$ trajectories are generated by increasingly precise nonnegative integer representatives $z_r = [-26 \cdot 217^{-1}]_{2^{9(r+1)}}$ of the negative 2-adic fixed point $z^*$.

---

## 4. State Typology & Certification Conditions

1. **Canonical Prefix State**: $(B_s, A_s^{\text{odd}}, \rho_s, y_s)$ with $0 \le \rho_s < 2^{B_s}$. Initial block lift $\Lambda_1(\epsilon) = C_{j_0} > 0$ selects source cylinder $\rho_1 = C_{j_0}$.
2. **Concrete Orbit State**: Stores full initial integer $z_0 = C_{j_0} + M_{j_0} n_0$ and current trajectory point $z_m$.
3. **Stabilized Source Tail State**: Certified by $\rho_s \equiv z_0 \pmod{2^{B_s}}$ AND $0 \le z_0 < 2^{B_s}$, proving that all higher source bits are strictly zero.

---

## 5. Verification Status

- **Status**: `VERIFIED_LOCAL_ZERO_LIFT_TRANSITION` & `SOUND_ACCELERATED_UNRANKED` (Global Dynamics).
- **Workspace Tests**: `cargo test --workspace` (**ALL PASSED**).
- **Clippy**: `cargo clippy --workspace --all-targets -- -D warnings` (**0 warnings**).
- **Python Oracle**: `scripts/zero_lift_oracle.py` (**0 diff**).
