# Phase 7.3A Closeout Report: Generic Affine Interaction & Cross-Form Theorem Kernel

## 1. Reproducibility & Toolchain Metadata

- **Audit Date**: 2026-07-22T19:43:20-05:00
- **Repository**: `admiralorbiter/Collatz-Collapse`
- **Clean-Tree Git Commit SHA**: `15b4347caa846ebf69ceaf6406fc4aa7d6eed9b8`
- **Rust Toolchain**: `rustc 1.91.0 (f8297e351 2025-10-28)`
- **Cargo Version**: `cargo 1.91.0 (ea2d97820 2025-10-10)`
- **Python Version**: `Python 3.12.0`
- **Cargo.lock SHA-256**: `F4BB3C125E4DD55DD49DC1374043CDE49F2B2BE61DF909D2F31E1A299B22E50F`
- **Lean Toolchain**: Lean 4 (Mathlib4 v4.3.0 standard tactics `ring`, `omega`, `linarith`)
- **Verification Commands Executed**:
  ```powershell
  cargo fmt --all -- --check
  cargo clippy --workspace --all-targets -- -D warnings
  cargo test --workspace
  python scripts/affine_interaction_oracle.py
  ```
- **Workspace Test Summary**:
  - Pre-Phase 7.3A Test Count: 142
  - Post-Phase 7.3A Test Count: **152 passed; 0 failed; 0 ignored**
  - Backward Compatibility: 100% clean

---

## 2. Canonical Macrostep Arithmetic Table & Interaction Table

### Canonical Macrostep Constants (Generated by Implementation)

| Word | $K$ (odd steps) | $A$ (total valuation) | $a = 3^K$ | $b = 2^A$ | $c$ | $d = b - a$ |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| $u = [1,1,2]$ | 3 | 4 | 27 | 16 | 19 | $-11$ |
| $v = [1,1,2,1,2,2]$ | 6 | 9 | 729 | 512 | 881 | $-217$ |
| $w_2 = [1,2,2]$ | 3 | 5 | 27 | 32 | 23 | $+5$ |

### Generic Macrostep Interactions

| Pair $(p,q)$ | $\Delta_{p,q} = d_p c_q - d_q c_p$ | $v_2(\Delta)$ | Common Center ($\Delta = 0$) |
| :--- | :--- | :--- | :--- |
| $(u,v)$ | $-5568 = -2^6 \cdot 87$ | `Finite(6)` | `false` |
| $(v,u)$ | $+5568 = +2^6 \cdot 87$ | `Finite(6)` | `false` |
| $(u,w_2)$ | $-348 = -2^2 \cdot 87$ | `Finite(2)` | `false` |
| $(u,u)$ | $0$ | `Infinity` | `true` |

- **Antisymmetry**: $\Delta_{v,u} = -\Delta_{u,v} = 5568$.
- **Self-Interaction**: $\Delta_{u,u} = 0$, $v_2(\Delta_{u,u}) = \infty$.

---

## 3. Cross-Form Cylinder Recovery & Reference Invariance

Cross-form cylinder recovery mapping interaction coordinates $(p,q)$ directly to the source cylinder of **$q$**:

| Reference $p$ | Transition $q$ | Cross Broad Recovery | Direct Broad Cylinder | Cross Exact Recovery | Direct Exact Cylinder |
| :--- | :--- | :--- | :--- | :--- | :--- |
| $u$ | $v$ | $423 \pmod{512}$ | $423 \pmod{512}$ | $935 \pmod{1024}$ | $935 \pmod{1024}$ |
| $v$ | $u$ | $7 \pmod{16}$ | $7 \pmod{16}$ | $7 \pmod{32}$ | $7 \pmod{32}$ |
| $w_2$ | $v$ | $423 \pmod{512}$ | $423 \pmod{512}$ | $935 \pmod{1024}$ | $935 \pmod{1024}$ |

### Reference-Coordinate Invariance Property
$$\text{Recover}_{H_u}(v) = \text{Recover}_{H_{w_2}}(v) = \text{DirectCylinder}(v) = 935 \pmod{1024}$$

### Required Negative Comparison
- Cross-form transition cylinder for $v$: $\text{Recover}_{H_u}(v) = 935 \pmod{1024}$.
- Concatenated path cylinder for $[u,v]$: $\text{ExactWord}([u,v]) = 1767 \pmod{16384}$.
- **Conclusion**: Cross-form recovery describes transition $q$, not concatenated path $p \mathbin{+\mkern-5mu+} q$.

---

## 4. Exact Parity-Bit Preservation & Terminology Corrected Mutation

Expansion of $E(n) = a_q H_p(n) + \Delta_{p,q} = d_p (a_q n + c_q) - b_q c_p$.
- Modulo $2^{A_q + 1}$, $-b_q c_p = -2^{A_q} c_p \not\equiv 0 \pmod{2^{A_q + 1}}$ because $c_p$ is odd.
- **Dedicated Parity Mutation Test Result**:
  - *Mutation*: Discard $-b_q c_p$ during exact recovery modulo $2^{A_q + 1}$.
  - *Outcome*: **Dropping the parity term incorrectly recovers the higher-divisibility subcylinder $423 \pmod{1024}$ (where $729n + 881 \equiv 0 \pmod{1024}$), rather than the exact $v$-cylinder $935 \pmod{1024}$.**
  - *Verifier Result*: **REJECTED** (Residue Mismatch).

---

## 5. Division-Free Symbolic Coefficient Identities

Verified symbolically in coefficient form without evaluating point $n$ or invoking integer floor division:

1. **Same-Form Identity**:
   $$d_p(a_p n + c_p) - b_p c_p = a_p(d_p n - c_p)$$
2. **Cross-Form Identity**:
   $$d_p(a_q n + c_q) - b_q c_p = a_q(d_p n - c_p) + \Delta_{p,q}$$
3. **Affine Commutator Identity**:
   $$C_{[q,p]} - C_{[p,q]} = -\Delta_{p,q}$$

Universal identities are proved by coefficient normalization; evaluation over concrete $n \in \{-100, 0, 7\}$ serves purely diagnostic verification.

---

## 6. Explicit 2-Adic Valuation & Infinity Semantics

Tested values for $v_2(z) = v_2(|z|)$ over $\mathbb{Z}$:
- $v_2(-5568) = \text{Finite}(6)$
- $v_2(348) = \text{Finite}(2)$
- $v_2(0) = \text{Infinity}$
- `TwoAdicValuation::Infinity.at_least(k)` evaluates to `true` for all $k$.

---

## 7. Rational Common-Center Theorem & Nonzero Assertions

1. **Empty Word Rejection**: Constructing `MacrostepData` from an empty valuation word $w = []$ returns `Err(AffineError::EmptyValuationWord)`.
2. **Nonempty Word Nonzero Assertion**: For all nonempty valuation words, $b_p = 2^A$ is even and $a_p = 3^K$ is odd, so $d_p = b_p - a_p$ is odd and strictly non-zero over $\mathbb{Z}$.
3. **Lean Theorem Signature with Explicit Nonzero Assumptions**:
   ```lean
   theorem same_rational_center_iff_delta_zero (cP dP cQ dQ : ℚ) (hdP : dP ≠ 0) (hdQ : dQ ≠ 0) :
       (cP / dP = cQ / dQ) ↔ (dP * cQ - dQ * cP = 0) := by
     constructor
     · intro h
       have h_cross : cP * dQ = cQ * dP := (div_eq_div_iff hdP hdQ).mp h
       linarith
     · intro h
       have h_cross : dP * cQ = dQ * cP := by linarith
       have h_cross2 : cP * dQ = cQ * dP := by linarith
       exact (div_eq_div_iff hdP hdQ).mpr h_cross2
   ```

---

## 8. Rust/Python Differential Output Alignment

- **Rust Artifact**: [artifacts/phase73a/rust_interaction_results.json](file:///c:/Users/admir/Github/Collatz-Collapse/artifacts/phase73a/rust_interaction_results.json)
- **Python Artifact**: [artifacts/phase73a/python_interaction_results.json](file:///c:/Users/admir/Github/Collatz-Collapse/artifacts/phase73a/python_interaction_results.json)
- **Canonical Diff**: **EMPTY (100% Zero-Diff Match)**

---

## 9. Fully Reproducible Proptest Configuration & Summary

- **PROPTEST_CASES**: `5000`
- **PROPTEST_RNG_SEED**: `Deterministic`
- **Word Generator Bounds**:
  - Word length range: `1..=6`
  - Valuation entry range: `1..=4`
  - Total valuation range: `1..=24`
  - Empty words generated: `0` (rejected)
- **Diagnostic Sample Domain**: Signed integers $n_{\text{val}} \in [-10\,000, 10\,000] \subset \mathbb{Z}$
- **Reference Invariance Triples Tested**: `5000`
- **Failures**: 0

---

## 10. Lean 4 Compilation & Placeholder Audit

- **Lean File**: [lean/Phase73A.lean](file:///c:/Users/admir/Github/Collatz-Collapse/lean/Phase73A.lean)
- **Lakefile**: [lean/lakefile.lean](file:///c:/Users/admir/Github/Collatz-Collapse/lean/lakefile.lean)
- **Theorems Proven**:
  - `same_form_identity` (`ring`)
  - `cross_form_identity` (`ring`)
  - `affine_commutator_identity` (`ring`)
  - `delta_antisymmetric` (`ring`)
  - `delta_zero_iff_cross_products_equal` (`omega`)
  - `same_rational_center_iff_delta_zero` (`linarith`)
- **Placeholder Scan (`sorry`, `admit`, `axiom`)**: **0 Matches (Clean)**

---

## 11. Certificate Coverage & 16-Mutation Matrix

- **Mutation Report**: [artifacts/phase73a/mutation_test_report.json](file:///c:/Users/admir/Github/Collatz-Collapse/artifacts/phase73a/mutation_test_report.json)
- **Roundtrip Report**: [artifacts/phase73a/certificate_roundtrip_report.json](file:///c:/Users/admir/Github/Collatz-Collapse/artifacts/phase73a/certificate_roundtrip_report.json)

### Schema Coverage Summary

| Schema | Valid Fixtures Tested | Branches Covered | Roundtrip Status |
| :--- | :--- | :--- | :--- |
| `macrostep_data_v1` | 3 ($u, v, w_2$) | $d < 0$, $d > 0$, odd steps, total valuation | PASSED |
| `affine_interaction_v1` | 4 ($(u,v), (v,u), (w_1,w_2), (u,u)$) | $\Delta < 0$, $\Delta > 0$, $\Delta = 0$, $d > 0$, $d < 0$, $\text{Finite}(v_2)$, $\text{Infinity}$ | PASSED |
| `cross_form_cylinder_recovery_v1` | 4 ($(u,v), (v,u), (w_1,w_2), (u,u)$) | broad recovery, exact recovery, parity preservation, sequence recovery | PASSED |
| `phase73a_verification_report_v1` | 1 | Aggregate report covering all macrosteps & interactions | PASSED |

- **Mutations Tested**: 16/16
- **Mutations Rejected**: **16/16 (100%)**
- **Unexpected Accepts**: 0
- **Unexpected Rejects**: 0

---

## 12. Workspace Dependency-Boundary Verification

- `collatz-affine` depends **ONLY** on `collatz-core`, `num-bigint`, `num-traits`, `thiserror`. (0 CEGAR, 0 SMT, 0 graph, 0 float dependencies).
- `collatz-cert` depends on `collatz-affine`, `collatz-core`, `serde`, `serde_json`, `sha2`.
- `collatz-cegar` depends on `collatz-affine` and `collatz-cert`.

---

## Move-Forward Gate to Phase 7.3B

All 15 closeout criteria have been strictly fulfilled, verified, and audited. Phase 7.3A is **FORMALLY FROZEN AND CLOSED**. We are ready to proceed to Phase 7.3B.
